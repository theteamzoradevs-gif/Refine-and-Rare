# Database (Neon / Vercel Postgres / local PostgreSQL)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/refine_and_rare?schema=public"

# Admin auth
JWT_SECRET="replace-with-a-long-random-string"
ADMIN_EMAIL="admin@refineandrare.com"
ADMIN_PASSWORD="change-me"

# Email (Resend) — leave blank to skip sending (enquiries still stored in DB)
RESEND_API_KEY=""
ENQUIRY_TO_EMAIL="rajasharma9226@gmail.com"
ENQUIRY_FROM_EMAIL="Refine & Rare <onboarding@resend.dev>"

# Site
NEXT_PUBLIC_SITE_URL="https://your-domain.com"

# Cloudinary (admin uploads) — leave blank to use local /public paths only
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=""
